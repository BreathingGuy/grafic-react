import { createSelectionStore } from './createSelectionStore';

/**
 * Selection store для offset таблицы (Q1 следующего года)
 */
export const useOffsetSelectionStore = createSelectionStore('OffsetSelectionStore');
